spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        default_batch_fetch_size: 10 // native query의 JOIN FETCH 미지원 문제

jwt:
  secret: ${JWT_SECRET_KEY}
  access-expiration: 1800000
  refresh-expiration: 1209600000
